# Editor Automático — FFmpeg Cloud Server V4
Servidor Express + FFmpeg nativo para Render. Inclui CORS para permitir que o GitHub Pages converse com o servidor. API assíncrona: POST /api/jobs, GET /api/jobs/:id e download em /api/jobs/:id/videos/:vid.
